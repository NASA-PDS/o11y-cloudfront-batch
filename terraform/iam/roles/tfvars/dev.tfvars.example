# IAM roles — dev venue
# Copy to dev.tfvars and fill in values.
# Shared values (aws_region, tenant, component, cicd) are in common-dev.tfvars.

venue     = "pds-cds-dev"
managedby = "<your-email>"

permissions_boundary_arn = "<arn:aws:iam::<account-id>:policy/<boundary-policy-name>>"
